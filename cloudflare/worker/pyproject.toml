[project]
name = "zwillingstag-worker"
version = "1.0.0"
description = "Zwillingstag CDU/CSU Digital Twin – Cloudflare Python Worker"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "pydantic",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
